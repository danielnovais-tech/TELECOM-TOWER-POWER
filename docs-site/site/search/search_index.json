{"config":{"lang":["pt","en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TELECOM TOWER POWER","text":"<p>API de planejamento de enlaces de r\u00e1dio com dados ANATEL em tempo real.</p> <p></p>"},{"location":"#o-que-e","title":"O que \u00e9","text":"<p>Uma API REST que responde \u00e0 pergunta que todo ISP, consultor RF e operadora se faz todo dia:</p> <p>\"Existe uma torre que possa atender este endere\u00e7o, com linha de visada e margem de enlace suficiente?\"</p> <p>Entregamos a resposta em milissegundos, consultando 140.906 torres georreferenciadas (ANATEL + OpenCelliD), cruzando com relevo SRTM de 90 m e \u2014 opcionalmente \u2014 produzindo uma an\u00e1lise t\u00e9cnica em linguagem natural via IA.</p>"},{"location":"#em-30-segundos","title":"Em 30 segundos","text":"<pre><code>curl -X POST https://api.telecomtowerpower.com.br/analyze_link \\\n  -H \"X-API-Key: demo_ttp_free_2604\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"receiver\": {\"lat\": -20.5, \"lon\": -41.9, \"height\": 5},\n    \"frequency_mhz\": 700,\n    \"max_distance_km\": 30\n  }'\n</code></pre> <p>Resposta resumida:</p> <pre><code>{\n  \"best_tower\": {\n    \"operator\": \"VIVO\",\n    \"distance_km\": 6.8,\n    \"azimuth_deg\": 142,\n    \"los_clear\": true,\n    \"fresnel_ok\": true,\n    \"path_loss_db\": 106.2,\n    \"link_margin_db\": 18.3\n  },\n  \"alternatives\": [ ... 5 more towers ... ]\n}\n</code></pre>"},{"location":"#proximos-passos","title":"Pr\u00f3ximos passos","text":"<ul> <li>Quickstart \u2014 primeira chamada em 5 minutos</li> <li>Autentica\u00e7\u00e3o \u2014 como obter e usar uma chave de API</li> <li>Refer\u00eancia da API \u2014 todos os endpoints</li> </ul>"},{"location":"api/overview/","title":"Vis\u00e3o geral da API","text":"<p>Base URL: <code>https://api.telecomtowerpower.com.br</code></p> <p>OpenAPI: /openapi.json \u00b7 Swagger UI: /docs</p>"},{"location":"api/overview/#endpoints-principais","title":"Endpoints principais","text":"M\u00e9todo Caminho Prop\u00f3sito POST <code>/analyze_link</code> Analisa um receptor contra todas as torres no raio POST <code>/plan_repeater</code> Encontra cadeia multi-saltos para cen\u00e1rio NLOS POST <code>/plan_repeater/async</code> Vers\u00e3o ass\u00edncrona com job id GET <code>/job/{id}</code> Status e resultado de job ass\u00edncrono POST <code>/batch</code> Processa CSV de receptores (tier Pro+) GET <code>/towers</code> Lista/filtra torres por bbox, operadora, banda POST <code>/ai/explain_link</code> Explica\u00e7\u00e3o em linguagem natural (tier Pro+) GET <code>/report/{id}.pdf</code> PDF do relat\u00f3rio (tier Starter+) GET <code>/health</code> Healthcheck <p>Detalhes de cada endpoint nas pr\u00f3ximas p\u00e1ginas.</p>"},{"location":"getting-started/authentication/","title":"Autentica\u00e7\u00e3o","text":"<p>Todas as requisi\u00e7\u00f5es exigem o header <code>X-API-Key</code>. As chaves s\u00e3o criadas no portal ap\u00f3s a assinatura de um plano.</p>"},{"location":"getting-started/authentication/#chaves-de-demo","title":"Chaves de demo","text":"<p>Chaves p\u00fablicas rotacionadas mensalmente \u2014 ideais para avaliar a API antes de assinar.</p> <ul> <li>Prefixo: <code>demo_try_\u2026</code></li> <li>Taxa: 6 req/min (cap forte, independente do tier)</li> <li>Sem exporta\u00e7\u00e3o PDF, sem IA</li> <li>Respostas carregam o header <code>X-Demo-Key: true</code></li> </ul>"},{"location":"getting-started/authentication/#chaves-de-producao","title":"Chaves de produ\u00e7\u00e3o","text":"<ul> <li>Prefixo: <code>ttp_</code> seguido de 32 caracteres aleat\u00f3rios (<code>secrets.token_urlsafe(32)</code>)</li> <li>Entregues via email no momento do checkout</li> <li>Persistentes: n\u00e3o rotacionam automaticamente \u2014 gire manualmente via portal se suspeitar de vazamento</li> </ul>"},{"location":"getting-started/authentication/#boas-praticas","title":"Boas pr\u00e1ticas","text":"<ul> <li>Nunca comprometa uma chave no front-end. Use um proxy backend.</li> <li>Configure <code>IP allowlist</code> no plano Enterprise.</li> <li>Monitore o header <code>X-RateLimit-Remaining</code> antes de parar de receber <code>429</code>.</li> </ul>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":""},{"location":"getting-started/quickstart/#1-obtenha-uma-chave-de-api","title":"1. Obtenha uma chave de API","text":"<p>Para testar agora mesmo use uma chave p\u00fablica de demo (10 req/min, sem PDF, sem IA):</p> <pre><code>X-API-Key: demo_ttp_free_2604\n</code></pre> <p>Para produ\u00e7\u00e3o, crie uma conta e escolha um plano.</p>"},{"location":"getting-started/quickstart/#2-faca-sua-primeira-chamada","title":"2. Fa\u00e7a sua primeira chamada","text":"<pre><code>curl -X POST https://api.telecomtowerpower.com.br/analyze_link \\\n  -H \"X-API-Key: demo_ttp_free_2604\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"receiver\": {\"lat\": -15.78, \"lon\": -47.93, \"height\": 10},\n    \"frequency_mhz\": 1800\n  }'\n</code></pre>"},{"location":"getting-started/quickstart/#3-interprete-o-resultado","title":"3. Interprete o resultado","text":"<p>A API retorna a melhor torre candidata com <code>link_margin_db</code>. Valores acima de 10 dB geralmente indicam enlace vi\u00e1vel em c\u00e9u claro; acima de 20 dB suporta chuva intensa nas bandas 700/1800/2100 MHz.</p>"},{"location":"getting-started/quickstart/#4-proximos-passos","title":"4. Pr\u00f3ximos passos","text":"<ul> <li>Veja a refer\u00eancia da API para todos os endpoints dispon\u00edveis.</li> <li>Configure autentica\u00e7\u00e3o para produ\u00e7\u00e3o.</li> <li>Leia sobre limites de taxa antes de colocar em produ\u00e7\u00e3o.</li> </ul>"},{"location":"getting-started/rate-limits/","title":"Limites de taxa","text":"<p>Cada chave tem uma janela deslizante de 60 segundos. Ao ser excedida a API devolve <code>429 Too Many Requests</code>.</p> Plano Req/min Chamadas/m\u00eas Lote (receptores) Free 10 200 \u2014 Starter 30 3.000 100 Pro 100 25.000 500 Business 300 150.000 2.000 Enterprise 1.000+ customizado 10.000"},{"location":"getting-started/rate-limits/#headers-de-resposta","title":"Headers de resposta","text":"<ul> <li><code>X-RateLimit-Limit</code> \u2014 teto de req/min vigente</li> <li><code>X-RateLimit-Remaining</code> \u2014 quantas ainda cabem na janela</li> <li><code>Retry-After</code> \u2014 presente em respostas 429</li> </ul>"},{"location":"getting-started/rate-limits/#chaves-de-demo","title":"Chaves de demo","text":"<p>Cap adicional de 6 req/min independente do tier nominal da chave.</p>"},{"location":"operations/production-status/","title":"Production Status","text":"<p>Snapshot of the production environment. Last verified: April 2026.</p>"},{"location":"operations/production-status/#infrastructure","title":"Infrastructure","text":"<ul> <li>EC2 + Docker Compose stable on instance <code>i-045166a6a1933f507</code> (<code>sa-east-1</code>, user <code>ubuntu</code>, path <code>/home/ubuntu/TELECOM-TOWER-POWER</code>).</li> <li>Instance type: <code>t3.small</code> (2 vCPU, 2 GB RAM, 20 GB gp3).<ul> <li>Load average live: <code>0,13 / 0,14 / 0,15</code> em 2 cores \u2014 CPU folgada.</li> <li>Mem\u00f3ria: <code>1,9 GB total</code>, <code>~800 MB dispon\u00edveis</code>, <code>~200 MB swap em uso</code> \u2014 est\u00e1vel, por\u00e9m justo.</li> <li>Disco: <code>15 GB / 19 GB em uso (82 %)</code> \u2014 monitorado pelo alerta <code>disk-space-low</code>.</li> </ul> </li> <li>Caddy listens on <code>:80</code> behind the ALB (which terminates TLS). Routing rules:<ul> <li><code>api.telecomtowerpower.com.br</code> \u2192 Railway (<code>https://web-production-90b1f.up.railway.app</code>) with <code>Host</code> header override.</li> <li><code>www.*</code> / <code>app.*</code> API paths (<code>/api/*</code>, <code>/health*</code>, <code>/calculate*</code>, <code>/towers*</code>, <code>/batch*</code>, <code>/jobs*</code>, <code>/docs*</code>, <code>/openapi.json</code>, <code>/stripe*</code>, <code>/usage*</code>, <code>/api-key*</code>, <code>/signup*</code>, <code>/login*</code>, <code>/profile*</code>, <code>/portal*</code>, <code>/analyze*</code>, <code>/plan_repeater*</code>, <code>/export_report*</code>, <code>/bedrock*</code>, <code>/srtm*</code>) \u2192 Railway.</li> <li><code>/webhook*</code> \u2192 local Stripe handler on <code>localhost:8001</code>.</li> <li><code>/grafana*</code> \u2192 local Grafana on <code>localhost:3001</code>.</li> <li>Fallback \u2192 React SPA on <code>localhost:3000</code>.</li> <li><code>docs.telecomtowerpower.com.br</code> \u2192 static MkDocs build served from <code>/srv/docs</code> inside the Caddy container.</li> </ul> </li> <li>ALB direct bypass (no Caddy):<ul> <li><code>monitoring.telecomtowerpower.com.br</code> \u2192 target group <code>ttp-grafana-tg:3001</code>.</li> <li><code>prometheus.telecomtowerpower.com.br</code> \u2192 target group <code>ttp-prometheus-tg:9090</code>.</li> </ul> </li> </ul>"},{"location":"operations/production-status/#cicd","title":"CI/CD","text":"<p>Three hardened workflows drive production operations:</p> Workflow Purpose <code>deploy-ec2-docker.yml</code> Build + push API image, SSM-deploy Docker Compose stack <code>update-ec2-stripe-secrets.yml</code> Sync Stripe secrets to EC2 via SSM <code>update-ec2-alerting-secrets.yml</code> Sync Slack webhook + SES SMTP credentials to EC2 <p>All three use:</p> <ul> <li>BuildKit cache mount \u2014 <code>RUN --mount=type=cache,id=pip-cache,target=/root/.cache/pip</code> in the Dockerfile.</li> <li><code>concurrency:</code> groups \u2014 prevent overlapping runs.</li> <li>Retry loop on health checks \u2014 30 iterations \u00d7 2 s against <code>localhost:3001/api/health</code> (Grafana) after secret sync.</li> </ul> <p>All 11 workflows under <code>.github/workflows/</code> are pinned to Node 24.</p>"},{"location":"operations/production-status/#observability","title":"Observability","text":"<p>Grafana provisioning (verified live via <code>/api/v1/provisioning/*</code>, <code>provenance: file</code>):</p> <ul> <li>Contact points<ul> <li><code>email-alerts</code> \u2192 <code>daniel.novais@sempreceub.com</code> (AWS SES SMTP)</li> <li><code>slack-alerts</code> \u2192 <code>#alerts</code> (via webhook)</li> </ul> </li> <li>Notification policies<ul> <li><code>severity=critical</code> \u2192 Slack (<code>continue: true</code>) + Email, <code>group_wait: 15s</code>, <code>repeat_interval: 1h</code></li> <li><code>severity=warning</code> \u2192 Slack + Email, <code>group_wait: 30s</code>, <code>repeat_interval: 2h</code></li> </ul> </li> <li>Alert rules (active)<ul> <li><code>high-5xx-rate</code> (critical) \u2014 <code>&gt;10</code> erros 5xx/min por 2 min</li> <li><code>HighRateLimitHits</code> (warning) \u2014 <code>&gt;5</code> 429/min por 2 min</li> <li><code>HighAPILatencyP95</code> (warning) \u2014 p95 <code>&gt;1,5 s</code> por 5 min</li> <li><code>BatchQueueStuck</code> (critical) \u2014 fila com jobs pendentes e throughput zero por 10 min</li> <li><code>batch-job-failures</code> (critical) \u2014 <code>&gt;1</code> falha em 5 min</li> <li><code>LowDiskSpace</code> (warning) \u2014 root fs <code>&gt;85 %</code> por 5 min</li> <li><code>memory-pressure</code> (warning) \u2014 swap <code>&gt;512 MB</code> por 10 min</li> </ul> </li> </ul> <p>M\u00e9tricas de host s\u00e3o expostas por um container <code>node-exporter</code> (<code>network_mode: host</code>) acess\u00edvel pelo Prometheus em <code>host.docker.internal:9100</code>.</p>"},{"location":"operations/production-status/#secrets","title":"Secrets","text":"<ul> <li>Never committed to git (<code>.gitignore</code> covers <code>secrets/</code>).</li> <li>Stored in AWS SSM Parameter Store (SecureString).</li> <li>Synced to EC2 at <code>/home/ubuntu/TELECOM-TOWER-POWER/secrets/</code> via GitHub Actions.</li> <li>Consumed by Docker Compose via the top-level <code>secrets:</code> mapping and mounted read-only at <code>/run/secrets/&lt;name&gt;</code>.</li> </ul> <p>Current files on EC2:</p> File Size Consumer <code>slack_webhook_url</code> 81 B Grafana Alertmanager <code>ses_smtp_username</code> 20 B Grafana Alertmanager (SMTP) <code>ses_smtp_password</code> 44 B Grafana Alertmanager (SMTP) <code>stripe_secret_key</code> \u2014 API container <code>stripe_webhook_secret</code> \u2014 Stripe webhook service"},{"location":"operations/production-status/#deployment","title":"Deployment","text":"<ul> <li>Zero-downtime \u2014 <code>aws ssm send-command</code> triggers <code>docker compose pull &amp;&amp; docker compose up -d</code> on the EC2 host. Compose performs rolling replacement per service.</li> <li>No SSH keys in CI \u2014 all remote execution goes through SSM.</li> <li>Railway/Railpack compatibility preserved \u2014 <code>railway.json</code>, <code>Procfile</code>, and <code>Dockerfile</code> remain valid; API image can be redeployed to Railway without changes.</li> </ul>"},{"location":"operations/runbook/","title":"Runbook","text":"<p>Operational procedures for the production EC2 environment.</p>"},{"location":"operations/runbook/#quick-reference","title":"Quick reference","text":"Resource Value EC2 instance ID <code>i-045166a6a1933f507</code> Region <code>sa-east-1</code> SSH user <code>ubuntu</code> Project path on host <code>/home/ubuntu/TELECOM-TOWER-POWER</code> Secrets path on host <code>/home/ubuntu/TELECOM-TOWER-POWER/secrets/</code> Caddy entry point <code>:80</code> (ALB terminates TLS) Railway API <code>https://web-production-90b1f.up.railway.app</code>"},{"location":"operations/runbook/#common-operations","title":"Common operations","text":""},{"location":"operations/runbook/#deploy-latest-main-to-ec2","title":"Deploy latest main to EC2","text":"<p>Trigger the workflow:</p> <pre><code>gh workflow run deploy-ec2-docker.yml\n</code></pre> <p>The workflow builds, pushes the API image, and runs <code>docker compose pull &amp;&amp; docker compose up -d</code> on EC2 via SSM. Rolling replacement is performed per service \u2014 no manual downtime window needed.</p>"},{"location":"operations/runbook/#sync-stripe-secrets-to-ec2","title":"Sync Stripe secrets to EC2","text":"<pre><code>gh workflow run update-ec2-stripe-secrets.yml\n</code></pre> <p>Reads <code>STRIPE_SECRET_KEY</code> and <code>STRIPE_WEBHOOK_SECRET</code> from GitHub Actions secrets and writes them to <code>/home/ubuntu/TELECOM-TOWER-POWER/secrets/</code>. Containers consuming these secrets are restarted automatically.</p>"},{"location":"operations/runbook/#sync-alerting-secrets-slack-ses-to-ec2","title":"Sync alerting secrets (Slack + SES) to EC2","text":"<pre><code>gh workflow run update-ec2-alerting-secrets.yml\n</code></pre> <p>Syncs <code>SLACK_WEBHOOK_URL</code>, <code>SES_SMTP_USERNAME</code>, <code>SES_SMTP_PASSWORD</code>. After sync, Grafana is restarted and a health check is retried up to 30 \u00d7 2 s against <code>localhost:3001/api/health</code>.</p>"},{"location":"operations/runbook/#reload-caddy-after-a-config-change","title":"Reload Caddy after a config change","text":"<pre><code>gh workflow run deploy-caddy.yml\n</code></pre> <p>Copies the <code>Caddyfile</code> to EC2 and runs <code>caddy reload</code>. Verifies HTTP 200 on <code>www.*</code>, <code>app.*</code>, <code>api.*</code>, <code>monitoring.*</code>, and <code>prometheus.*</code>.</p>"},{"location":"operations/runbook/#troubleshooting","title":"Troubleshooting","text":""},{"location":"operations/runbook/#grafana-health-check-fails-during-alerting-secrets-workflow","title":"Grafana health check fails during alerting-secrets workflow","text":"<ul> <li>Ensure the port is 3001 on the EC2 host (not 3000 \u2014 that's the React frontend).</li> <li>SSH in and check: <code>docker compose logs grafana --tail=50</code>.</li> <li>Verify the secret files exist and are non-empty: <code>ls -la /home/ubuntu/TELECOM-TOWER-POWER/secrets/</code>.</li> </ul>"},{"location":"operations/runbook/#502-on-apitelecomtowerpowercombr","title":"502 on <code>api.telecomtowerpower.com.br</code>","text":"<ul> <li>First check Railway status \u2014 the Caddy config proxies 100% of API traffic there.</li> <li>Check Caddy logs on EC2: <code>docker compose logs caddy --tail=100</code>.</li> <li>Confirm ALB target is healthy: AWS console \u2192 Target Groups \u2192 <code>ttp-caddy-tg</code>.</li> </ul>"},{"location":"operations/runbook/#5xx-alert-fires","title":"5xx alert fires","text":"<p>The <code>high-5xx-rate</code> alert triggers when the API emits more than 10 errors/minute over a 1-minute window. Steps:</p> <ol> <li>Check Grafana dashboard for the affected endpoint.</li> <li><code>docker compose logs api --tail=200</code> on EC2, or check Railway logs if traffic is routed there.</li> <li>If the spike is from a specific customer, check rate limits in <code>key_store.json</code>.</li> </ol>"},{"location":"operations/runbook/#monitoring-subdomain-returns-404502","title":"Monitoring subdomain returns 404/502","text":"<ul> <li><code>monitoring.*</code> and <code>prometheus.*</code> bypass Caddy and hit ALB target groups directly.</li> <li>Verify targets are healthy: <code>ttp-grafana-tg</code> (port 3001), <code>ttp-prometheus-tg</code> (port 9090).</li> <li>If the EC2 instance was replaced, re-register it with <code>scripts/manage_ec2_alb.sh register</code>.</li> </ul>"},{"location":"operations/runbook/#security-reminders","title":"Security reminders","text":"<ul> <li>Secrets are never committed. If a secret leaks into git, rotate it immediately and force-push a history rewrite only as a last resort \u2014 prefer rotation.</li> <li>All remote EC2 execution goes through SSM; no SSH keys are configured in CI.</li> <li>Secret files on EC2 are mounted read-only at <code>/run/secrets/&lt;name&gt;</code> inside containers.</li> </ul>"},{"location":"en/","title":"TELECOM TOWER POWER","text":"<p>Radio link planning API backed by live ANATEL data.</p> <p></p>"},{"location":"en/#what-it-is","title":"What it is","text":"<p>A REST API that answers the question every ISP, RF consultant and carrier asks every day:</p> <p>\"Is there a tower that can serve this address, with line of sight and enough link margin?\"</p> <p>We return the answer in milliseconds by querying 140,906 geolocated towers (ANATEL + OpenCelliD), cross-referencing SRTM 90m terrain, and \u2014 optionally \u2014 producing a natural-language technical statement via AI.</p>"},{"location":"en/#in-30-seconds","title":"In 30 seconds","text":"<pre><code>curl -X POST https://api.telecomtowerpower.com.br/analyze_link \\\n  -H \"X-API-Key: demo_ttp_free_2604\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"receiver\": {\"lat\": -20.5, \"lon\": -41.9, \"height\": 5},\n    \"frequency_mhz\": 700,\n    \"max_distance_km\": 30\n  }'\n</code></pre> <p>Trimmed response:</p> <pre><code>{\n  \"best_tower\": {\n    \"operator\": \"VIVO\",\n    \"distance_km\": 6.8,\n    \"azimuth_deg\": 142,\n    \"los_clear\": true,\n    \"fresnel_ok\": true,\n    \"path_loss_db\": 106.2,\n    \"link_margin_db\": 18.3\n  },\n  \"alternatives\": [ ... 5 more towers ... ]\n}\n</code></pre>"},{"location":"en/#next","title":"Next","text":"<ul> <li>Quickstart \u2014 first call in 5 minutes</li> <li>Authentication \u2014 how to get and use an API key</li> <li>API reference \u2014 every endpoint</li> </ul>"},{"location":"en/api/overview/","title":"API overview","text":"<p>Base URL: <code>https://api.telecomtowerpower.com.br</code></p> <p>OpenAPI: /openapi.json \u00b7 Swagger UI: /docs</p>"},{"location":"en/api/overview/#main-endpoints","title":"Main endpoints","text":"Method Path Purpose POST <code>/analyze_link</code> Analyze a receiver against every tower in range POST <code>/plan_repeater</code> Find multi-hop chain for NLOS scenarios POST <code>/plan_repeater/async</code> Async variant with job id GET <code>/job/{id}</code> Status + result of an async job POST <code>/batch</code> Process a CSV of receivers (Pro+ tier) GET <code>/towers</code> List/filter towers by bbox, operator, band POST <code>/ai/explain_link</code> Natural-language explanation (Pro+ tier) GET <code>/report/{id}.pdf</code> PDF report (Starter+ tier) GET <code>/health</code> Healthcheck <p>See the next pages for per-endpoint details.</p>"},{"location":"en/getting-started/authentication/","title":"Authentication","text":"<p>Every request needs the <code>X-API-Key</code> header. Keys are created in the portal once a plan is active.</p>"},{"location":"en/getting-started/authentication/#demo-keys","title":"Demo keys","text":"<p>Public keys rotated monthly \u2014 great for evaluating the API before subscribing.</p> <ul> <li>Prefix: <code>demo_try_\u2026</code></li> <li>Rate: 6 rpm (hard cap regardless of tier claimed)</li> <li>No PDF, no AI</li> <li>Responses carry an <code>X-Demo-Key: true</code> header</li> </ul>"},{"location":"en/getting-started/authentication/#production-keys","title":"Production keys","text":"<ul> <li>Prefix: <code>ttp_</code> followed by 32 random characters (<code>secrets.token_urlsafe(32)</code>)</li> <li>Delivered by email at checkout</li> <li>Persistent: they do not rotate automatically \u2014 rotate manually in the portal if compromised</li> </ul>"},{"location":"en/getting-started/authentication/#best-practices","title":"Best practices","text":"<ul> <li>Never ship a key in frontend code. Use a backend proxy.</li> <li>Enterprise plans support IP allowlists.</li> <li>Watch the <code>X-RateLimit-Remaining</code> header to avoid <code>429</code> responses.</li> </ul>"},{"location":"en/getting-started/quickstart/#1-get-an-api-key","title":"1. Get an API key","text":"<p>Try it now with a public demo key (10 rpm, no PDF, no AI):</p> <pre><code>X-API-Key: demo_ttp_free_2604\n</code></pre> <p>For production, sign up and pick a plan.</p>"},{"location":"en/getting-started/quickstart/#2-first-call","title":"2. First call","text":"<pre><code>curl -X POST https://api.telecomtowerpower.com.br/analyze_link \\\n  -H \"X-API-Key: demo_ttp_free_2604\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"receiver\": {\"lat\": -15.78, \"lon\": -47.93, \"height\": 10},\n    \"frequency_mhz\": 1800\n  }'\n</code></pre>"},{"location":"en/getting-started/quickstart/#3-read-the-result","title":"3. Read the result","text":"<p>The API returns the best candidate tower with <code>link_margin_db</code>. Values above 10 dB usually mean the link is viable in clear sky; above 20 dB tolerates heavy rain in the 700 / 1800 / 2100 MHz bands.</p>"},{"location":"en/getting-started/quickstart/#4-next","title":"4. Next","text":"<ul> <li>Browse the API reference for every available endpoint.</li> <li>Set up authentication for production.</li> <li>Read about rate limits before going to production.</li> </ul>"},{"location":"en/getting-started/rate-limits/","title":"Rate limits","text":"<p>Every key uses a sliding 60-second window. When exceeded the API returns <code>429 Too Many Requests</code>.</p> Plan Rpm Calls/mo Batch receivers Free 10 200 \u2014 Starter 30 3,000 100 Pro 100 25,000 500 Business 300 150,000 2,000 Enterprise 1,000+ custom 10,000"},{"location":"en/getting-started/rate-limits/#response-headers","title":"Response headers","text":"<ul> <li><code>X-RateLimit-Limit</code> \u2014 current rpm ceiling</li> <li><code>X-RateLimit-Remaining</code> \u2014 calls still available in the window</li> <li><code>Retry-After</code> \u2014 present on 429 responses</li> </ul>"},{"location":"en/getting-started/rate-limits/#demo-keys","title":"Demo keys","text":"<p>Additional hard cap of 6 rpm regardless of the nominal tier of the key.</p>"},{"location":"en/operations/production-status/#infrastructure","title":"Infrastructure","text":"<ul> <li>EC2 + Docker Compose stable on instance <code>i-045166a6a1933f507</code> (<code>sa-east-1</code>, user <code>ubuntu</code>, path <code>/home/ubuntu/TELECOM-TOWER-POWER</code>).</li> <li>Instance type: <code>t3.small</code> (2 vCPU, 2 GB RAM, 20 GB gp3).<ul> <li>Live load average: <code>0.13 / 0.14 / 0.15</code> on 2 cores \u2014 CPU well under capacity.</li> <li>Memory: <code>1.9 GB total</code>, <code>~800 MB available</code> with <code>~200 MB swap in use</code> \u2014 stable but tight.</li> <li>Disk: <code>15 GB / 19 GB used (82 %)</code> \u2014 monitored by the <code>disk-space-low</code> alert.</li> </ul> </li> <li>Caddy listens on <code>:80</code> behind the ALB (which terminates TLS). Routing rules:<ul> <li><code>api.telecomtowerpower.com.br</code> \u2192 Railway (<code>https://web-production-90b1f.up.railway.app</code>) with <code>Host</code> header override.</li> <li><code>www.*</code> / <code>app.*</code> API paths (<code>/api/*</code>, <code>/health*</code>, <code>/calculate*</code>, <code>/towers*</code>, <code>/batch*</code>, <code>/jobs*</code>, <code>/docs*</code>, <code>/openapi.json</code>, <code>/stripe*</code>, <code>/usage*</code>, <code>/api-key*</code>, <code>/signup*</code>, <code>/login*</code>, <code>/profile*</code>, <code>/portal*</code>, <code>/analyze*</code>, <code>/plan_repeater*</code>, <code>/export_report*</code>, <code>/bedrock*</code>, <code>/srtm*</code>) \u2192 Railway.</li> <li><code>/webhook*</code> \u2192 local Stripe handler on <code>localhost:8001</code>.</li> <li><code>/grafana*</code> \u2192 local Grafana on <code>localhost:3001</code>.</li> <li>Fallback \u2192 React SPA on <code>localhost:3000</code>.</li> <li><code>docs.telecomtowerpower.com.br</code> \u2192 static MkDocs build served from <code>/srv/docs</code> inside the Caddy container.</li> </ul> </li> <li>ALB direct bypass (no Caddy):<ul> <li><code>monitoring.telecomtowerpower.com.br</code> \u2192 target group <code>ttp-grafana-tg:3001</code>.</li> <li><code>prometheus.telecomtowerpower.com.br</code> \u2192 target group <code>ttp-prometheus-tg:9090</code>.</li> </ul> </li> </ul>"},{"location":"en/operations/production-status/#observability","title":"Observability","text":"<p>Grafana provisioning (verified live via <code>/api/v1/provisioning/*</code>, <code>provenance: file</code>):</p> <ul> <li>Contact points<ul> <li><code>email-alerts</code> \u2192 <code>daniel.novais@sempreceub.com</code> (AWS SES SMTP)</li> <li><code>slack-alerts</code> \u2192 <code>#alerts</code> (via webhook)</li> </ul> </li> <li>Notification policies<ul> <li><code>severity=critical</code> \u2192 Slack (<code>continue: true</code>) + Email, <code>group_wait: 15s</code>, <code>repeat_interval: 1h</code></li> <li><code>severity=warning</code> \u2192 Slack + Email, <code>group_wait: 30s</code>, <code>repeat_interval: 2h</code></li> </ul> </li> <li>Alert rules</li> </ul> UID Severity Trigger <code>high-5xx-rate</code> critical <code>&gt;10</code> server errors/min for 2 min <code>HighRateLimitHits</code> warning <code>&gt;5</code> 429/min for 2 min <code>HighAPILatencyP95</code> warning p95 latency <code>&gt;1.5 s</code> for 5 min <code>BatchQueueStuck</code> critical pending jobs with zero throughput for 10 min <code>batch-job-failures</code> critical <code>&gt;1</code> batch job failure in 5 min <code>LowDiskSpace</code> warning root disk <code>&gt;85 %</code> for 5 min <code>memory-pressure</code> warning swap <code>&gt;512 MB</code> for 10 min <p>Host metrics are exposed via a <code>node-exporter</code> container (<code>network_mode: host</code>) reachable from Prometheus at <code>host.docker.internal:9100</code>.</p>"},{"location":"en/operations/production-status/#secrets","title":"Secrets","text":"<ul> <li>Never committed to git (<code>.gitignore</code> covers <code>secrets/</code>).</li> <li>Stored in AWS SSM Parameter Store (SecureString).</li> <li>Synced to EC2 at <code>/home/ubuntu/TELECOM-TOWER-POWER/secrets/</code> via GitHub Actions.</li> <li>Consumed by Docker Compose via the top-level <code>secrets:</code> mapping and mounted read-only at <code>/run/secrets/&lt;name&gt;</code>.</li> </ul> File Size Consumer <code>slack_webhook_url</code> 81 B Grafana / Alertmanager <code>ses_smtp_username</code> 20 B Grafana / Alertmanager (SMTP) <code>ses_smtp_password</code> 44 B Grafana / Alertmanager (SMTP) <code>stripe_secret_key</code> \u2014 API container <code>stripe_webhook_secret</code> \u2014 Stripe webhook service"},{"location":"en/operations/production-status/#deployment","title":"Deployment","text":"<ul> <li>Zero-downtime \u2014 <code>aws ssm send-command</code> triggers <code>docker compose pull &amp;&amp; docker compose up -d</code> on the EC2 host. Compose performs rolling replacement per service.</li> <li>No SSH keys in CI \u2014 all remote execution goes through SSM.</li> <li>Railway / Railpack compatibility preserved \u2014 <code>railway.json</code>, <code>Procfile</code>, and <code>Dockerfile</code> remain valid; the API image can be redeployed to Railway without changes.</li> </ul>"},{"location":"en/operations/runbook/#quick-reference","title":"Quick reference","text":"Resource Value EC2 instance ID <code>i-045166a6a1933f507</code> Region <code>sa-east-1</code> Instance type <code>t3.small</code> (2 vCPU, 2 GB RAM, 20 GB gp3) SSH user <code>ubuntu</code> Project path on host <code>/home/ubuntu/TELECOM-TOWER-POWER</code> Secrets path on host <code>/home/ubuntu/TELECOM-TOWER-POWER/secrets/</code> Caddy entry point <code>:80</code> (ALB terminates TLS) Railway API <code>https://web-production-90b1f.up.railway.app</code>"},{"location":"en/operations/runbook/#deploy-latest-main-to-ec2","title":"Deploy latest <code>main</code> to EC2","text":"<pre><code>gh workflow run deploy-ec2-docker.yml\n</code></pre> <p>The workflow builds, pushes the API image, and runs <code>docker compose pull &amp;&amp; docker compose up -d</code> on EC2 via SSM. Rolling replacement is performed per service \u2014 no manual downtime window needed.</p>"},{"location":"en/operations/runbook/#grafana-health-check-fails-during-the-alerting-secrets-workflow","title":"Grafana health check fails during the alerting-secrets workflow","text":"<ul> <li>Ensure the port is 3001 on the EC2 host (not 3000 \u2014 that\u2019s the React frontend).</li> <li>SSH in and check: <code>docker compose logs grafana --tail=50</code>.</li> <li>Verify the secret files exist and are non-empty: <code>ls -la /home/ubuntu/TELECOM-TOWER-POWER/secrets/</code>.</li> </ul>"},{"location":"en/operations/runbook/#502-on-apitelecomtowerpowercombr","title":"502 on <code>api.telecomtowerpower.com.br</code>","text":"<ul> <li>First check Railway status \u2014 the Caddy config proxies 100 % of API traffic there.</li> <li>Check Caddy logs on EC2: <code>docker compose logs caddy --tail=100</code>.</li> <li>Confirm the ALB target is healthy: AWS console \u2192 Target Groups \u2192 <code>ttp-caddy-tg</code>.</li> </ul>"},{"location":"en/operations/runbook/#5xx-alert-fires","title":"5xx alert fires","text":"<p>The <code>high-5xx-rate</code> alert triggers when the API emits more than 10 errors/minute over a 1-minute window. Steps:</p> <ol> <li>Check the Grafana dashboard for the affected endpoint.</li> <li><code>docker compose logs api --tail=200</code> on EC2, or check Railway logs if traffic is routed there.</li> <li>If the spike is from a specific customer, check rate limits in <code>key_store.json</code>.</li> </ol>"},{"location":"en/operations/runbook/#disk-space-low-alert-fires","title":"Disk-space-low alert fires","text":"<p>The EC2 root fs is 20 GB. When utilisation passes 85 %, this alert fires. Cleanup steps:</p> <pre><code># SSH in, then:\ndocker system prune -af --filter \"until=168h\"   # images &gt;7 days old\ndocker builder prune -af\nsudo journalctl --vacuum-time=7d\nsudo du -h /var/lib/docker/containers | sort -h | tail\n</code></pre> <p>If the disk still fills within weeks, increase the EBS volume (<code>aws ec2 modify-volume \u2026</code>) and run <code>sudo growpart /dev/nvme0n1 1 &amp;&amp; sudo resize2fs /dev/nvme0n1p1</code>.</p>"},{"location":"en/operations/runbook/#memory-pressure-alert-fires","title":"Memory-pressure alert fires","text":"<p>On <code>t3.small</code> (2 GB RAM) swap is expected under bursts, but sustained usage over 512 MB for 10+ minutes means real pressure. Options:</p> <ol> <li><code>docker stats --no-stream</code> to find the heaviest container.</li> <li>Temporary relief: restart the worst offender.</li> <li>Lasting relief: resize to <code>t3.medium</code> (4 GB) via <code>aws ec2 modify-instance-attribute</code>.</li> </ol>"},{"location":"en/operations/runbook/#monitoring-subdomain-returns-404-502","title":"Monitoring subdomain returns 404 / 502","text":"<ul> <li><code>monitoring.*</code> and <code>prometheus.*</code> bypass Caddy and hit ALB target groups directly.</li> <li>Verify targets are healthy: <code>ttp-grafana-tg</code> (port 3001), <code>ttp-prometheus-tg</code> (port 9090).</li> <li>If the EC2 instance was replaced, re-register it with <code>scripts/manage_ec2_alb.sh register</code>.</li> </ul>"},{"location":"en/operations/runbook/#security-reminders","title":"Security reminders","text":"<ul> <li>Secrets are never committed. If a secret leaks into git, rotate it immediately; prefer rotation over history-rewrite.</li> <li>All remote EC2 execution goes through SSM; no SSH keys are configured in CI.</li> <li>Secret files on EC2 are mounted read-only at <code>/run/secrets/&lt;name&gt;</code> inside containers.</li> </ul>"}]}