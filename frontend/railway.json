{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile"
    },
    "deploy": {
        "startCommand": "/docker-entrypoint.sh",
        "healthcheckPath": "/",
        "healthcheckTimeout": 60,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3,
        "envVars": {
            "BACKEND_URL": {
                "description": "Server-side URL the nginx container proxies to. Use Railway's private network: http://${{ web.RAILWAY_PRIVATE_DOMAIN }}:8000",
                "required": true
            },
            "VITE_API_URL": {
                "description": "Public HTTPS URL of the API, baked into the JS bundle at build time: https://${{ web.RAILWAY_PUBLIC_DOMAIN }} (or the custom domain).",
                "required": true
            }
        }
    }
}